# Cleanup notes

- Removed the legacy `DrivingViewModel` after migrating state handling to the refactored view models and use cases.
- Deleted the one-off PowerShell scaffolding script that was only used to bootstrap the Kotlin package layout.
